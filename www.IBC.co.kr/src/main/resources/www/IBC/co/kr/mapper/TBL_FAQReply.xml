<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="faqrns">

<select id="list" parameterType="integer" resultType="FAQReply">
	select * from TBL_FAQREPLY where fno=#{fno} order by rno
</select>

<insert id="insert" parameterType="FAQReply">
	<selectKey keyProperty="rno" order="BEFORE" resultType="integer">
		select nvl(max(rno),0) + 1 from TBL_FAQREPLY
	</selectKey>
	insert into TBL_FAQREPLY values (#{rno}, #{fno}, #{fid},
		 #{content}, sysdate, sysdate, 'n')
</insert>

<update id="delete" parameterType="integer">
	update TBL_FAQREPLY set del='y' where rno=#{rno}
</update>

<update id="update" parameterType="FAQReply">
	update TBL_FAQREPLY set content=#{content}, updatedate=sysdate
		where rno=#{rno}
</update>

</mapper>